Google Search:
[UI (Поисковая страница / API-клиенты)] -> [Server-side (индексация, сервисы запросов, микросервисы, балансировщики)] -> [Storage (шардированные индексы, объектное хранилище, реплики)]

Netflix:
[UI (web/tv/mobile клиенты)] -> [Server-side (API-шлюзы, микросервисы, рекомендации, контроллер потоковой передачи)] -> [Storage (CDN, объектное хранилище, метаданные в БД)]

Amazon (e‑commerce):
[UI (web/mobile клиенты)] -> [Server-side (каталог, корзина, заказы, поиск, платежи — множество сервисов)] -> [Storage (реляционные БД, NoSQL, поисковые индексы)]

GitHub:
[UI (web/CLI/API)] -> [Server-side (приложение + вспомогательные сервисы: git-хранилище, CI/CD, поиск)] -> [Storage (git-объекты, реляционная БД, блоб-хранилище)]

Slack:
[UI (web/desktop/mobile)] -> [Server-side (реалтайм-шлюз, брокеры сообщений, микросервисы)] -> [Storage (хранилище сообщений, метаданные в БД, кеши)]

Dropbox:
[UI (десктоп-клиент / web / mobile)] -> [Server-side (сервис синхронизации, разрешение конфликтов, API)] -> [Storage (объектное хранение файлов, метаданные в БД, версионирование)]

Wikipedia (MediaWiki):
[UI (веб-браузер)] -> [Server-side (MediaWiki PHP-приложение, слой кеширования)] -> [Storage (MySQL primary + реплики, memcached/varnish)]

WordPress.com:
[UI (посетитель/редактор web/mobile)] -> [Server-side (мульти‑тенантные WordPress-инстансы, инфраструктура плагинов)] -> [Storage (MySQL, объектное хранилище для медиа, CDN)]

Stripe:
[UI (дэшборд / клиенты / вебхуки)] -> [Server-side (API-шлюз, биллинг, антифрод, платежные сервисы)] -> [Storage (транзакционные БД, журнал событий, логи)]

IPFS:
[UI (веб-шлюзы / клиентские приложения)] -> [Server-side (локальный узел / HTTP-шлюз / индексирующие сервисы)] -> [Storage (DHT и peer-хранилища — content-addressed blocks, множество пиров)]